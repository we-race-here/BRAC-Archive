pipeline {
    agent {
        docker {
            image 'node:16.3.0-alpine'
            args '-p 3000:3000'
        }
    }
    environment {
        CI = 'true'
    }
    stages {
//         stage('Build') {
//             steps {
//                 sh 'npm install'
//                 sh 'npm run build'
//             }
//         }
        stage('Deploy') {
            steps {
                sh 'cd deployment'
                sh 'docker build -t wrh-brac:prd -f Dockerfile .'
//                 sh "if [ `echo $?` == 0 ]; then docker rm -f wrh-brac docker run -dt --restart=always -p 8010:8010 --name wrh-brac wrh-brac:prd fi"
            }
        }
    }
}